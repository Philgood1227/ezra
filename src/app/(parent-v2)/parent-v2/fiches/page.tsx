import { ParentFichesManager } from "@/features/fiches/components";

export default function ParentV2FichesPage(): React.JSX.Element {
  return (
    <section className="mx-auto w-full max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
      <ParentFichesManager />
    </section>
  );
}
